# Handoff — Punters Mate: track-record page live; market-blend is the next build

Date: 2026-06-15 (R14 done, R15 upcoming) · Repo:
`C:\Users\megan\OneDrive\Documents\Claude\afl-player-stats` (github.com/Dwayno44/afl-player-stats,
`main`, Pages → dwayno44.github.io/afl-player-stats/). User in Perth (AWST).

## Read first
- Prior handoffs for deep context (don't duplicate): `.claude/handoffs/2026-06-09-…`
  (strategic conclusion + fantasy-shadow spec), `…/2026-06-11-…`, `…/2026-06-14-…`.
- `TESTS.md` — the test register (read before proposing any feature; nearly everything's tested).
- `MODEL.md`, `BRAINSTORM.md`, `explainers/`.

## What's new this session (all committed/pushed)
- **Public track-record page — `docs/results.html`** (generated by `results.py`,
  linked from the main page banner "See how we've done"). Per game / per round /
  across all rounds, with the green-vs-amber breakdown. **Framing matters here and
  was iterated carefully:**
  - LEADS with the floor success (the core value), value-ROI demoted to a secondary
    "note on the value highlights"; green<amber kept as honest nuance ("favour amber").
  - Hero is **honest small-sample** language: "holding so far — in line with its
    ~85% design … early days, it'll move as the sample grows." Do NOT re-inflate to
    "comfortably above target" (it leaned on a 2-round hot streak).
  - **KEY DISTINCTION the user insisted on:** the 34k-game backtest is the model's
    *tuning/development* set; **live performance = only the rounds since the page
    went live (R13, R14)**. Never present the backtest as performance on this page.
    Backtest credentials + the full levers-tested overview are for a *separate future
    "about the model" page* (content already in TESTS.md / explainers/what-we-tested.md).
  - Self-maintaining: the **Monday `afl-weekly-grade` task now also runs `results.py`
    and pushes `docs/results.html`** (and `report-md`). Completed rounds cache to
    `results_data.json` (gitignored).
- **Market line as a projection INPUT** (`exp_market.py`) — **the headline model
  finding.** 50/50 blend of our disposal projection + the Sportsbet ladder's
  vig-adjusted implied median beats our blend: **−2.1% R13, replicated −2.2% R14**
  (two rounds, both positive). The only disposal accuracy gain found. Snapshots now
  capture `d_ladder` so it validates forward.
- **Line drift** (`exp_drift.py`) — "who does late money come for": carries bias info
  but sample thin (15 movers); `odds_log.jsonl` (gitignored) accumulating via
  `exp_drift.py log`. Revisit ~300 movers.
- **Weather** (`exp_weather.py`) — null for projections; last untested family closed.
- **Line-level disposals-against** (`exp_line_da.py`) — null (no persistence).
- **Green-only warning banner** on the main page (`matchup_app.py .headsup`) — live.
- **Live-game grading bug fixed** (`scorecard.py api_actuals` skips non-CONCLUDED) +
  **floor calibration logged** (`exp_floor_calib.py`, TESTS.md): backtest holds 85.0%
  all-players but **82.4% on the shown floor≥10 slate** (steady every season) — ~2.5pts
  optimistic; tweak candidate.

## R14 result (final)
Floors calibrated (~85% shown); value ROI negative; GREEN (~64%) << AMBER (~87%) again.
Monday's auto-grade has the full round + refreshes both `live-results.md` and `results.html`.

## THE NEXT BUILDS (priority order)
1. **Productionise the market-blend** — the only replicated disposal accuracy gain
   (~2%). Blend the page's disposal projection toward the Sportsbet implied median
   (vig-adjusted, p*≈0.60 per `exp_market.py`) where a ladder exists; fall back
   otherwise. **Tension to reconcile:** market-anchored floors shrink the green/amber
   tints toward zero by construction (consistent with "tints aren't edge," but decide
   how it sits with the new banner + track-record framing).
2. **Floor-z tweak** — close the ~2.5pt shown-slate gap (nudge z / fatten the tail so
   shown clears ~85%). Small, self-contained.
3. **Fantasy LGBM shadow** (`fantasy_lgbm.py`) — the −2.6% fantasy gain (spec in
   2026-06-09 handoff); fantasy ladders accruing since R14.
4. **Future "about the model" page** — published `docs/` version of the credentials /
   levers-tested overview (content exists in TESTS.md + explainers/).

## Near-term ops
- **R15 publish:** props weren't posted yet (checked Sun — 0 priced). Publish the
  first live R15 odds view **Thursday onward** when markets drop: `matchup_app.py
  --odds` → `scorecard.py snapshot --round current` → push. (User said "wait".)

## Gotchas (plus prior handoffs)
- **Pull before push** — CI + the Monday job move the remote; a plain push is rejected.
  `git pull --rebase`; the recurring `error: Please commit or stash them` is just the
  always-dirty `.claude/handoffs/2026-06-05-monetisation…md` (another session) blocking
  the no-op pull — the push still fast-forwards. Stage files explicitly; never `git add -A`.
- **Two preview gotchas:** the main `index.html` is too heavy (1.9 MB) for
  `preview_screenshot` (times out) — verify it via grep + `preview_console_logs`. But
  `results.html` is light and screenshots fine (use `preview_eval` to navigate there
  first; server config `punters-mate-docs`, port 8137).
- **cp1252 console** — no unicode in `print()`; HTML entities fine in page strings.
- **afltables CSV round numbering is offset** (its "R13" = AFL R12) — grade via the AFL
  API, never the CSV. AFL API `verify=True`; Sportsbet AU-IP-only (odds builds local).
- `lightgbm` in venv but NOT in `requirements.txt` (use native `lgb.train`).
- Throwaways untracked: `floor_read.py`, `score_round*.py`, root icon files.

## Suggested skills
- **None required** — next steps are direct coding (`matchup.py`/`matchup_app.py`) + CLI.
- `/handoff` again if you stop mid-build.
- `anthropic-skills:consolidate-memory` if the project memory files have drifted.
